# Evidence snapshots are fingerprinted byte-for-byte and must not be rewritten
# by platform checkout line-ending conversion.
fixtures/knowledge/captured/*.txt -text

# Shell scripts run inside Linux containers from a Windows checkout: they
# must never take carriage returns on the way (2026-09-18).
*.sh text eol=lf
